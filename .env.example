# DocuMind Environment Configuration
# Copy this file to .env and fill in values if using cloud APIs

# Optional: Google Gemini API key (only needed for cloud LLM fallback)
# GOOGLE_API_KEY=your_key_here

# Ollama settings (defaults work for local installation)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3
